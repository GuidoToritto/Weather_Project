const NavBar = () => {
    return(
        <nav className="navbar bg-light text-light mb-5">
            <div className="container-fluid">
                <h3 className="mx-auto text-muted"> Weather prediction project</h3>
            </div>
        </nav>
    );
}

export default NavBar;